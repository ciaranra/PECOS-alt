# protoprpy
[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "proto_bytemesage_py"
version = "0.6.0.dev8 "
requires-python = ">=3.8,<3.13"
readme = "README.md"
dependencies = []
classifiers = [
    "Programming Language :: Rust",
    "Programming Language :: Python :: Implementation :: CPython",
]
